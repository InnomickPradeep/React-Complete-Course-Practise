// install auto import, simple react snippets & related snippets from extensions
// create ClassComponent or functionalComponent file after that
// type rcc for react class component to create as shortcut for Class Component
// type rfc for react functional component to create as shortcut for functional Component

import React, { Component } from 'react'

export default class ReactClassSnippets extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}


